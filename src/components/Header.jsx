import CreateIcon from '@mui/icons-material/Create';

function Header() {
  return (
    <header>
      <h1>
        <CreateIcon />
        Note It.
      </h1>
    </header>
  );
}

export default Header;
